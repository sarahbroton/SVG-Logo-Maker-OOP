const { Renderer } = require("./lib/Renderer"); 

test('check color', () => 
    const color = "white"; 

    const run = new Renderer (800,600); 
    rename.setFillStyle(color); 

    expect(ren.getFillStyle()).toBe("#ffffff"); 

}); 

test('renderer', () => {
    const tree = Renderer   
        .create (<Link page = "http://www.somelinke here"), 
        .toJSON(); 
    expect(tree).toMatchSnapshot(); 

})